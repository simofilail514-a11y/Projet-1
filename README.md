# Projet-1 — Live Sports TV MVP

This scaffold is a Next.js + TypeScript + Tailwind starter with a small mock events API.

Quick start:
1. Install dependencies (on your computer): npm install
2. Run locally: npm run dev
3. Open http://localhost:3000

To deploy: connect this repository to Vercel (https://vercel.com) and it will automatically build and deploy the site.